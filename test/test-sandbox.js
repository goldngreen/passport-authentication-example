
const assert = require('assert');


describe('sandbox-1', () => {

    it('should be easy to run test code', () => {
    });

    it('should be easy to split up test code', () => {
    });

    it('should be easy to isolate test code', () => {
    });
});